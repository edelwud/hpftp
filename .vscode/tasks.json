{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake",
            "type": "shell",
            "command": "cd ./dist && cmake ../../FTP && cmake --build . && clear && ./FTPServer",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}